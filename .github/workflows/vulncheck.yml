name: vulncheck

on:
  workflow_dispatch:
  schedule:
    - cron: '0 9 * * 6'

env:
  GO_VERSION: '1.25.0'

jobs:
  vulncheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - uses: dhth/composite-actions/.github/actions/govulncheck@main
        with:
          go-version: ${{ env.GO_VERSION }}
